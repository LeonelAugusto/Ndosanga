import 'package:flutter/material.dart';

class PaginaPerdido extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('perdidos',
      style: TextStyle(
        fontSize: 20.0,
       ),
      ),
    );
  }
  }